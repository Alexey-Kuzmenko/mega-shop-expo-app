import { Typography } from '@/shared';
import { View } from 'react-native';

function Notifications() {
    return (
        <View>
            <Typography text='Notifications screen' />
        </View>
    );
}

export default Notifications;
